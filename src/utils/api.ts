import { useSupabaseServer } from "@/utils/supabase/server";

// export async function getConfigPage(page: string) {
//     // Create a Supabase client instance
//   const supabase = useSupabaseBrowser();

//   const { data, error } = await supabase.rpc("get_config_page", {
//     page,
//   });

//   if (error) {
//     throw error;
//   }

//   return data;
// }
